<?php
/** @var \Magento\Framework\View\Element\Template $block */
/** @var \Magento\Framework\Escaper $escaper */

use Hyva\Theme\ViewModel\HeroiconsOutline;
use Hyva\Theme\ViewModel\HeroiconsSolid;

/** @var HeroiconsOutline|null $icons */
$icons = $viewModels->require(HeroiconsOutline::class);

$homeUrl = $block->getUrl('');
?>

<!-- Subtle background section similar to the lower parts of the page -->
<section class="w-full">
  <div class="mx-auto max-w-5xl w-full px-5 py-14">

    <!-- Minimal card centered -->
    <div class="mx-auto max-w-2xl rounded-2xl text-center">

      <div class="mx-auto mb-3 flex h-14 w-14 items-center justify-center rounded-2xl">
        <?php if ($icons): ?>
          <span class="text-[#586cac]">
            <?= $icons->exclamationCircleHtml('w-12 h-12'); ?>
          </span>
        <?php endif; ?>
      </div>

      <h1 class="text-3xl font-extrabold tracking-tight">
        <?= $escaper->escapeHtml(__('404 – Page Not Found')) ?>
      </h1>

      <p class="mt-2 text-[15px] leading-relaxed text-slate-700">
        <?= $escaper->escapeHtml(__('We’re sorry, the link is invalid or has been moved.')) ?>
      </p>

      <!-- CTA buttons in website style -->
      <div class="mt-6 flex flex-col sm:flex-row items-center justify-center gap-3">
        <a href="<?= $escaper->escapeUrl($homeUrl) ?>"
           class="inline-flex items-center gap-2 rounded-xl btn-primary px-5 py-3 text-sm font-semibold btn ring-1 transition-all duration-200 active:translate-y-[1px]">
          <?= $escaper->escapeHtml(__('Back to homepage')) ?>
        </a>
      </div>

    </div>
  </div>
</section>
